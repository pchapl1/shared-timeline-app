export type MapMemory = {
  id: number;
  title: string;
  latitude: number;
  longitude: number;
};

export type MapTrip = {
  id: number;
  title: string;
  latitude: number;
  longitude: number;
};